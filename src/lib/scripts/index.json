[
  {
    "name": "check_versions",
    "spec_section": "Versions",
    "description": "Validates the `version` column format and uniqueness within a COD-AB boundary file.",
    "applies_to": ["all"],
    "implementations": [
      {
        "language": "javascript",
        "file": "src/lib/scripts/check_versions.js",
        "runtime": "browser / duckdb-wasm"
      }
    ],
    "output": "{ passed: bool, violations: string[], warnings: string[], info: string[] }"
  }
]
